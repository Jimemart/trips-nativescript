export const SET_EMAIL = (state, value) => {
  state.email = value
}

export const SET_PASS = (state, value) => {
  state.password = value
}

export const SET_REPEAT = (state, value) => {
  state.repeat = value
}

export const SET_PHONE = (state, value) => {
  state.phone = value
}
